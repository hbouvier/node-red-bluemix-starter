{
    "name"         : "iot-bluemix",
    "version"      : "0.6.0",
    "dependencies": {
        "when": "~3.x",
        "mongodb": "~1.4.x",
        "nano": "6.2.x",
        "bcrypt": "1.0.2",
        "cfenv":"~1.0.0",
        "express":"4.x",
        "body-parser":"1.x",
        "http-shutdown":"1.2.0",
        "node-red-studio":"0.17.5-2",
        "node-red-dashboard":"2.4.3",
        "node-red-auth-github":"0.1.0"
    },
    "scripts": {
        "start": "for filename in templates/*.tpl ; do sed 's^${ROOT}^'${HTTP_ADMIN_ROOT:-${HTTP_ROOT:-/}}'^g' ${filename} > public/$(basename ${filename%.tpl}) ; done && node --max-old-space-size=192 index.js --settings ./bluemix-settings.js -v"
    },
    "engines": {
        "node": "8.x"
    }
}